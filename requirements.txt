# ── MCP SOC — Python Dependencies ──────────────────────────────────────────

# API / Broker
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# Async worker queue (uses Redis — replaces Celery)
arq>=0.26.0

# Redis Streams (event bus between agents)
redis[hiredis]>=5.0.0

# Async MongoDB driver
motor>=3.5.0
pymongo>=4.8.0

# AWS (CloudTrail ingestion + S3 log storage)
boto3>=1.35.0

# Schema validation (used in every agent)
pydantic[email]>=2.8.0
pydantic-settings>=2.4.0

# Sigma detection rules
pySigma>=0.11.0

# MITRE ATT&CK mapping (bundled STIX data, no live TAXII calls)
mitreattack-python>=3.0.0

# Graph-based correlation
networkx>=3.3

# Anthropic Claude API (Investigation Agent only)
anthropic>=0.40.0

# Email alerting
fastapi-mail>=1.4.0
sendgrid>=6.11.0

# Jinja2 email templates
jinja2>=3.1.0

# Firebase Admin SDK (JWT verification)
firebase-admin>=6.5.0

# bcrypt for API key hashing
bcrypt>=4.0.0

# GeoIP enrichment (Ingestion Agent)
geoip2>=4.8.0

# Environment / config
python-dotenv>=1.0.0

# HTTP client (for enrichment calls)
httpx>=0.27.0

# ── Testing ─────────────────────────────────────────────────────────────────
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
pytest-cov>=5.0.0

# ── Linting / Formatting ────────────────────────────────────────────────────
ruff>=0.6.0
